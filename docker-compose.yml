services:
  nntp-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: nntp_web_client
    restart: always
    ports:
      - "3001:3001"
    volumes:
      - ./src/data:/app/src/data
    env_file:
      - .env
    environment:
      - NODE_ENV=production
      - PORT=3001
      - ADMIN_USERNAME=geehong
      - ADMIN_PASSWORD=Power@6740
      - NNTP_HOST=news.usenet.farm
      - NNTP_PORT=563
      - NNTP_SSL=true
      - NNTP_USER=ufadh3njs4xtxy0e
      - NNTP_PASS=c35y18s53qglwx5e
      - VIPER_HOST=news.vipernews.com
      - VIPER_PORT=563
      - VIPER_SSL=true
      - VIPER_USER=geecgpia@gmail.com
      - VIPER_PASS=Power@6740
      - VIPER_MAX_CONNECTIONS=5
    networks:
      - default
      - shared-network

networks:
  shared-network:
    external: true
